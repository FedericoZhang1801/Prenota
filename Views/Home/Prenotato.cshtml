@model List<Utente>
@{
    ViewData["Title"] = "Prenotato";
}

<h2>Grazie</h2>
<p>per esserti Registrato</p>

<p>Sono stati Registrati @Model.Count() utenti...</p>

<table class="table">
<thead class="thead-dark">
    <th>Nome</th>
    <th>Cognome</th>
    <th>Email</th>
    <th>Città</th>
</thead>
@{ 
    foreach(var item in @Model) {
    <tr>
        <td>@item.Nome</td>
        <td>@item.Cognome</td>
        <td>@item.Email</td>
        <td>@item.Città</td>
    </tr>
    }
}

</table>